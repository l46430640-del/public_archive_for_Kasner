# Methods and normalizations

## Einstein-Maxwell-scalar model

We use signature `(-,+,+,+)`, geometrized units, and

\[
S=\frac{1}{16\pi}\int d^4x\sqrt{-g}
\left[R-2(\nabla\psi)^2-Z(\psi)F^2\right],
\qquad Z(\psi)=e^{\psi^2}.
\]

The horizon radius is one. The canonical scalar is
`varphi=sqrt(2) psi`. Generalized Kasner exponents obey

\[
p_1+p_2+p_3=1,
\qquad
p_1^2+p_2^2+p_3^2+p_\varphi^2=1.
\]

For the spherically symmetric critical family,

\[
p_s=\frac{2}{\beta^2+3},\quad
p_t=\frac{\beta^2-1}{\beta^2+3},\quad
p_\varphi=\frac{2\sqrt2\beta}{\beta^2+3},
\]

with `beta=c0/sqrt(delta)[1+o(1)]` and
`delta=q/q_c-1`.

## Scalarization threshold and backgrounds

Linearizing the scalar equation on Reissner-Nordstrom and compactifying with
`u=1/r` gives

\[
\partial_u[(1-u)(1-Q_H^2u)\partial_u\psi]+Q_H^2\psi=0.
\]

The horizon-regular solution is a Legendre function of degree
`-1/2+i sqrt(3)/2`. Its source vanishes when

\[
\operatorname{Re}P_{-1/2+i\sqrt3/2}
\left(\frac{1+Q_H^2}{1-Q_H^2}\right)=0,
\qquad
q_c=\frac{2Q_H}{1+Q_H^2}.
\]

The nonlinear scalarized branch is shot on the compact interval `u in [0,1]`.
The interior continuation uses `x=log z` and selects the first sustained
Kasner interval whose normalized background residual is below `10^-6`.

## Regular odd transfer

For `ell=2`, the odd gravitational and electromagnetic canonical variables
form a two-component field `Psi`. The radial problem is

\[
\partial_{r_*}^2\Psi+[\omega^2-V_{\rm odd}(r)]\Psi=0,
\]

where `V_odd` is the real symmetric potential generated by the quadratic EMS
action. At the future horizon the two basis solutions carry unit ingoing
symplectic flux. At the Kasner matching surface, reconstruction of the
tangential magnetic field gives the row `T_B` and

\[
\mathcal A_B=\eta_H T_B(\delta,\omega)u.
\]

The frequency derivative is integrated together with the radial equation. Its
norm bounds the variation of `T_B` around `omega M=0.2`. The public data give a
positive lower bound on `I_*=[0.195,0.205]` and on the geodesic ball of radius
`0.1` around the maximum-transfer direction in `CP^1`.

## Vanishing-energy packet and peak clock

The positive-frequency packet is

\[
a_+(v)=\eta_H e^{-v^2/(2T_H^2)}e^{-i\omega v},
\qquad a_{\rm real}=\sqrt2\,\operatorname{Re}a_+.
\]

For

\[
\eta_H=\delta^{1/2+\kappa},\qquad
T_H=\tau\delta^{-1/2},\qquad \kappa>0,
\]

its flux energy is

\[
E_H=\sqrt\pi\,\tau\delta^{1/2+2\kappa}\longrightarrow0.
\]

During a free Kasner flight the magnetic wall fraction is

\[
\mathcal R_B(t)=|\mathcal A_B|^2
e^{-2p_st+[(\psi_K+vt)^2-\psi_K^2]},
\qquad v=p_\varphi/\sqrt2.
\]

The positive root of `R_B=1` yields

\[
t_{\rm peak}\sim\delta^{-1/2}\sqrt{L_B},
\qquad L_B=\ln(1/|\mathcal A_B|).
\]

The numerical observable is
`Theta_peak=t_peak sqrt(delta)/sqrt(L_B)`, measured from the geometric Kasner
matching surface to the maximum of the nonlinear wall.

## Contracting layer and reflection

The old coordinate `x=sqrt(delta)(s-s_peak)` has width
`O(L_B^-1/2)`. The regular inner variables are

\[
q_B=\frac{|\varphi_*|}{2}\sim\sqrt{L_B},\qquad
z=q_B\sqrt\delta(s-s_{\rm peak}),\qquad
\chi=q_B(\varphi-\varphi_*).
\]

The leading scalar-wall system is

\[
\partial_z\widehat\pi_\varphi=-2\widehat\Omega_B,
\quad
\partial_z\chi=\widehat\pi_\varphi,
\quad
\partial_z\ln\widehat\Omega_B=2\widehat\pi_\varphi.
\]

Its exact solution is a `sech^2` wall pulse with a `tanh` momentum profile.
Consequently,

\[
\mathcal R_\infty:
(\widehat p_1,\widehat p_2,\widehat p_3,\widehat p_\varphi)
\mapsto
(\widehat p_1,\widehat p_2,\widehat p_3,-\widehat p_\varphi),
\]

with finite-wall corrections of order `q_B^-1=O(L_B^-1/2)`.

The committed trajectories solve the original one-wall Hamiltonian, record
both Kasner plateaus, and compare them with the independently rescaled
finite-wall connection map. Bounded and constant couplings use the same
incoming Kasner data and magnetic input.

## Scope

These equations determine the first isolated Taub-degenerate magnetic
transition reached from regular odd horizon data. A broader EMS evolution has
the same local scattering limit when its additional generalized-Kasner shift
satisfies

\[
\|\Delta P_{\rm ext}\|/\sqrt\delta\longrightarrow0.
\]

